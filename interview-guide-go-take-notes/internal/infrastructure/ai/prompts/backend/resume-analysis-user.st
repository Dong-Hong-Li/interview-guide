# Input
请站在“后端岗位技术简历评审”的视角，基于以下简历内容输出结构化审计结果。

## Candidate Resume
---resume start---
{resumeText}
---resume end---

注意：上述简历文本可能来自 OCR PDF 转文本，存在错字、换行错位、符号缺失、字段串行、术语乱码等抽取噪声。请先结合上下文进行合理还原，再做评审；不要因为文本质量差、排版破碎或局部乱码而直接负面判断候选人能力。若确实无法判断，请明确说明是 OCR/抽取噪声导致的信息缺失。

## Review Context
本次评审聚焦后端 / 服务端 / 基础设施方向，请重点关注以下能力是否在简历中被真实、清晰地体现：
- 服务端项目复杂度与业务闭环
- 缓存、数据库、消息队列、搜索、分布式任务等中间件使用深度
- 并发处理、性能优化、稳定性治理、可观测性、故障排查能力
- 接口设计、数据建模、系统拆分、异步化、幂等、一致性等工程能力
- 技术表达是否体现“问题背景、方案动作、结果收益”，而不是单纯堆砌技术名词

## Optimization Benchmarks
你可以参考以下“成熟后端项目表述方式”来判断候选人当前描述是否到位，但这些内容仅作为优化方向参考，不能写成候选人已完成的既成事实。

### 高并发与缓存
- 多级缓存：例如 Redis + Caffeine 构建本地与分布式缓存协同策略，并说明如何处理缓存击穿、穿透、雪崩。
- 原子控制：例如使用 Redis Lua 脚本实现限流、扣减、去重等原子性场景。

### 异步化与性能优化
- 异步编排：例如对多路 RPC / IO 请求进行并发编排，缩短接口整体 RT。
- 线程治理：例如线程池隔离、参数调优、任务堆积治理、死锁排查。
- SQL / 存储优化：例如索引优化、慢查询治理、批处理、冷热分层。

### 微服务与一致性
- 数据链路：例如 MySQL 增量同步、消息驱动解耦、搜索索引异步刷新。
- 最终一致性：例如基于消息队列、重试、补偿、幂等控制处理跨服务状态一致性。
- 网关与安全：例如统一鉴权、权限模型、限流熔断、灰度发布。

### 复杂业务建模
- 领域建模：例如抽象核心实体、流程状态、规则引擎、责任链、策略模式等。
- 流程控制：例如订单、审批、履约、库存等复杂状态流转与幂等治理。

### 稳定性与治理
- 可观测性：例如日志、指标、链路追踪、告警、压测、Dump 分析。
- 容量与可靠性：例如分库分表、削峰填谷、降级容灾、批处理优化。

## Output Requirements
1. 直接输出 JSON 对象，不要输出任何 Markdown 代码块。
2. 严格遵守既定 JSON 字段，不要新增字段，不要遗漏字段。
3. 如果简历中缺少关键信息，请在 `missingInfo` 中指出。
4. 如果原始项目描述较弱，请在 `rewrites` 中给出更专业的改写版本，并明确优化原因。
5. 评分应克制、可信，与简历内容强相关，不要为了“好看”而给高分。
6. 若扣分或指出问题，请区分“候选人内容本身不足”和“OCR/文本抽取噪声导致无法识别”，不要混为一谈。
