# Input
请站在“前端岗位技术简历评审”的视角，基于以下简历内容输出结构化审计结果。评审口径必须明显偏前端，不要用后端或泛全栈视角替代前端分析。

## Candidate Resume
---resume start---
{resumeText}
---resume end---

注意：上述简历文本可能来自 OCR PDF 转文本，存在错字、换行错位、符号缺失、字段串行、术语乱码等抽取噪声。请先结合上下文进行合理还原，再做评审；不要因为文本质量差、排版破碎或局部乱码而直接负面判断候选人能力。若确实无法判断，请明确说明是 OCR/抽取噪声导致的信息缺失。

## Review Context
本次评审聚焦前端工程能力，请重点判断候选人是否真实、清晰地体现了以下内容：
- 页面与交互复杂度，而不只是“完成页面开发”
- 组件抽象、状态管理、表单/表格/工作台等复杂 UI 设计能力
- 浏览器渲染、首屏加载、列表性能、资源加载、包体积优化等性能能力
- TypeScript、接口契约、异常处理、联调协作、可维护性治理等工程能力
- 测试体系、构建发布、组件库、设计系统、规范建设等团队工程化能力
- 移动端适配、H5、小程序、跨端方案、低代码、可视化等前端高价值场景
- 技术表达是否体现“业务/体验问题 + 技术动作 + 结果收益”，而不是单纯堆砌框架名称

## Optimization Benchmarks
你可以参考以下“成熟前端项目表述方式”来判断候选人当前描述是否到位，但这些内容仅作为优化方向参考，不能写成候选人已完成的既成事实。

### 性能与体验优化
- 首屏性能：例如路由级拆包、关键资源预加载、SSR / SSG / CSR 取舍、LCP 和白屏时间优化。
- 渲染性能：例如虚拟列表、分片渲染、防抖节流、长任务治理、避免无效重渲染。
- 资源策略：例如图片格式优化、CDN 缓存、按需加载、懒加载、缓存指纹与静态资源治理。

### 组件与状态设计
- 组件抽象：例如沉淀高复用业务组件、表单引擎、表格配置化、工作台模块化。
- 状态管理：例如按复杂度选择 Context、Redux、Zustand、Pinia、状态机等方案，并说明为何这样设计。
- 复杂交互：例如拖拽、可视化编排、富文本、权限驱动 UI、动态配置渲染。

### 工程化与质量建设
- 类型安全：例如 TypeScript 严格模式、接口类型收敛、运行时校验、低成本联调。
- 测试体系：例如组件测试、E2E、CI 门禁、回归场景覆盖。
- 构建发布：例如 Vite / Webpack 优化、Monorepo、微前端、灰度发布、错误监控。

### 协作与业务价值
- 协作效率：例如统一请求层、错误处理、权限模型、跨团队组件复用、设计规范统一。
- 结果收益：例如降低白屏时间、减少重复开发、提升交付效率、降低线上报错率、优化关键转化路径。

## Output Requirements
1. 直接输出 JSON 对象，不要输出任何 Markdown 代码块。
2. 严格遵守既定 JSON 字段，不要新增字段，不要遗漏字段。
3. 评分必须体现前端岗位标准，不要套用后端项目评价逻辑。
4. 如果简历中缺少关键前端信息，请在 `missingInfo` 中指出。
5. 如果原始项目描述较弱，请在 `rewrites` 中给出更专业的前端表达，并明确优化原因。
6. 评分应克制、可信，与简历内容强相关，不要为了“好看”而给高分。
7. 若扣分或指出问题，请区分“候选人内容本身不足”和“OCR/文本抽取噪声导致无法识别”，不要混为一谈。
