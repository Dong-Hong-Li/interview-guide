# Go 后端开发进度（`interview-guide-go-take-notes`）

本文件说明本仓库**当前**接口与模块状态，并参考 `interview-guide/docs/api-interface-statistics.md` 的「41 个前端业务接口」口径做对照。  
**勿**将下述路径理解为 Java 后端的 `modules/*` 或本仓库不存在的 `router/router.go`——**以本仓库下表代码路径为准**。

**文档同步日期**：2026-04-24（已含 `GET /api/interview/sessions` 列表与 `PUT .../answers` 草稿保存）。

---

## 1. 统计范围

- 对照说明：`interview-guide/docs/api-interface-statistics.md`（2+8+9+14+8，路径不重复计数，业务约 **41** 条，不含根路径 `/health`、`/`、`/api/meta` 等系统辅助口）。
- **本仓库在「41 口径 + 与主项目 Go 能力对齐」下的结论**：

| 类别 | 数量 | 说明 |
|------|------|------|
| **真实业务实现** | **20** | 见 §2 索引；简历 8 + 面试 12（含**会话分页列表**与 `PUT` **草稿**） |
| **固定 501 占位** | **22** | 知识库 14 + RAG 对话 8，见 §3 |
| **与主项目常见能力仍可能缺** | **1 类** | 见下表 |

| 与主项目常见列表仍不一致 | 本仓库 |
|--------------------------|--------|
| `GET /api/resumes/health` | **未**在 `ResumeController` 中注册；站点有 `GET /health`（`system` 包） |

- **对账（以代码为准）**：**20 真实 + 22 占位 = 42** 条有注册与明确返回；较「约 41」条是否完全重合取决于统计表是否把 `resumes/health`、或是否合并「POST/PUT 同路径不同方法」等.

---

## 1.1 本仓库代码布局（对照实现，勿抄 Java 目录）

| 区域 | 路径 | 说明 |
|------|------|------|
| 入口与依赖注入 | `cmd/server/main.go`、`cmd/server/deps.go`、`cmd/server/wire*.go` | 装配 Postgres / Redis / OpenAI、Wire 生成控制器；`RouteRegistrar` 含简历、面试、知识库 501、RAG 501 |
| HTTP 与全局路由 | `internal/interfaces/controller/router.go`、`internal/interfaces/controller/system` | `/api` 下挂载各模块；根 `GET /`、`GET /health` |
| 请求绑定 | `internal/interfaces/http/binding` | `Handle` / `Exec` 与主项目类名不同，语义一致 |
| 简历/面试/知识库/RAG 控制器 | `internal/application/<模块>/controller` | `api.go` 为路径片段；`Register` 内 `r.Route(APIMountPath, ...)`，完整 URL 为 `/api` + 挂载 + 子路径（如 `/api/knowledgebase/list` 见 `knowledgebase/controller`） |
| 入参 DTO | `internal/application/<模块>/model`（如 `request_param.go`） | |
| 应用服务 / 用例 | `internal/application/<模块>/service` | |
| 仓储端口与实现 | `internal/application/<模块>/repository`；`internal/infrastructure/postgres/mapper` | `InterviewMapper`、`ResumeMapper` 等 |
| 表与 SQL | 见仓库内 `schema` / `grom` 模型（以实际路径为准，非 `internal/db`） | |
| 对象存储 | `internal/infrastructure/storage` 等 | 简历上传经 `ResumeUploadService` |
| 简历分析 / 面试评估 Stream | `internal/infrastructure/redis/stream` | 与 `StartDeps` 中条件启动一致 |
| 面试报告 PDF | `internal/infrastructure/pdf`（`RenderInterviewReportPDF` 等） | 中文字体与简历导出共享探测逻辑/环境变量提示 |
| 访问日志 | `internal/interfaces/middleware/requestlog.go` | 可配合配置抑制规则 |

---

## 2. 已实现接口（本仓库索引，仅路径 → 主要入口）

行为与边界**以代码为准**；下表为**真实逻辑**（非 501）.

### 2.1 简历（`internal/application/resume/controller`）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/resumes/upload` | `uploadAndAnalyze` |
| GET | `/api/resumes/interviewer-roles` | `interviewerRoles`（辅助，未必计入 41） |
| GET | `/api/resumes` | `listResumes` |
| GET | `/api/resumes/statistics` | `getStatistics` |
| POST | `/api/resumes/{id}/reanalyze` | `reanalyzeResume` |
| GET | `/api/resumes/{id}/detail` | `getResumeDetail` |
| GET | `/api/resumes/{id}/export` | `handleExportAnalysisPDF`（raw，PDF） |
| DELETE | `/api/resumes/{id}` | `deleteResume` |

**未实现**：`GET /api/resumes/health`（与部分文档/主项目表不一致；若需对齐请补 `Register` 一行）。

### 2.2 模拟面试（`internal/application/interview/controller`）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/interview/sessions` | `createInterview` |
| GET | `/api/interview/sessions` | `listInterviewSessions`（`?page=&size=`，`InterviewListPage`） |
| GET | `/api/interview/sessions/unfinished/{resumeId}` | `findUnfinishedInterview` |
| GET | `/api/interview/sessions/{sessionId}/question` | `getCurrentQuestion` |
| GET | `/api/interview/sessions/{sessionId}/report` | `getReport` |
| GET | `/api/interview/sessions/{sessionId}/details` | `getInterviewDetail` |
| GET | `/api/interview/sessions/{sessionId}/export` | `handleExportInterviewPDF`（`application/pdf`） |
| DELETE | `/api/interview/sessions/{sessionId}` | `deleteInterview` |
| POST | `/api/interview/sessions/{sessionId}/answers` | `submitAnswer`（提交并推进游标） |
| PUT | `/api/interview/sessions/{sessionId}/answers` | `saveAnswer`（仅落库草稿 + 刷缓存，不推进、不入队） |
| POST | `/api/interview/sessions/{sessionId}/complete` | `completeInterview` |
| GET | `/api/interview/sessions/{sessionId}` | `getSession` |

---

## 3. 未完成接口（22，固定 501）

- **知识库 14**（`internal/application/knowledgebase/controller`）：`upload`、`list`、`{id}`、`{id}/download`、`delete`、`categories`、`category/{category}`、`uncategorized`、`{id}/category`、`search`、`stats`、`{id}/revectorize`、`query`、`query/stream`. 全部 `StatusNotImplemented` + `errmsg.NotImplemented: knowledgebase.*`.
- **RAG 8**（`internal/application/ragchat/controller`）：创建/列表/详情/流式消息/改标题/改知识库/置顶/删会话. 全部 `errmsg.NotImplemented: ragChat.*`.

---

## 4. 批次与验收（可执行顺序；**状态以本仓库为准**）

- **批次 A（简历 P0）**：本仓库**已具备**上传、列表、统计、详情、删、导出 PDF、重分析与消费者条件启动；与 §2.1 一致。✅
- **批次 B（面试主链 P0）**：创建/答题/交卷/报告/PDF/评估与出题路径以当前 `service` + `redis/stream` 为准；`GET .../export`、`getReport` 与门禁一致。✅
- **批次 C（历史与辅助 P1）**：`GET /api/interview/sessions` 分页列表（`ListInterviewSessionsService` + `InterviewMapper.ListInterviewSessionsPage`）、`GET .../details`、`DELETE`、`GET .../export`、`PUT .../answers`（草稿）已与 `history.ts` / 主项目 Go 语义对齐。✅
- **批次 D / E / F（知识库 + RAG）**：当前仅为 §3 的 501 占位，按文档顺序落地时可沿用接口形状。⏳

**横向能力**：Postgres/Redis/Wire/OpenAI 条件注入、面试评估与简历分析消费、`pdf` 与字体提示等，以 `cmd/server` 与 `internal/infrastructure` 为准。

---

## 5. 执行约定

- 更新本文档时：同步改 **同步日期**；「已实现数量」以 §1 表 + §2 枚举为准。  
- 若补齐 **GET** `/api/resumes/health`，应同步修改 §1 计数与 §2 表。  

**当前摘要**：与主项目 Go 面试域相比，仅 **`GET /api/resumes/health`** 仍常缺；知识库与 RAG 为 **全 501 占位，非真实业务**。
